# Shared deer-dataset block, pasted into each model config via mmengine _base_
# (kept as a reference; the per-model configs inline these values because list
# fields do not deep-merge across bases).
